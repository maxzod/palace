import 'response.dart';

extension ResponseWithView on Response {
  Future<void> view(String path, Map<String, dynamic> params) {
    throw UnimplementedError('response.view() is not finished yet');
  }
}
